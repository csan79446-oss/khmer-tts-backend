BACKEND_URL=
API_KEY=
DEFAULT_ENGINE=VoxCPM2
OUTPUT_DIRECTORY=
CONNECTION_TIMEOUT=30
POLLING_INTERVAL=2
SAVE_GENERATION_HISTORY=true
# Worker-side (RunPod env, not the desktop .env):
# LOAD_DENOISER=1        # loads ZipEnhancer to clean clone reference audio
# MAX_REFERENCE_AUDIO_SECONDS=10
# OUTPUT_SAMPLE_RATE=48000
